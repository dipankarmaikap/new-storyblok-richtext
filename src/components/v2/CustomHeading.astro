---
import type { RichTextComponentProps, StoryblokRichTextJson } from '../../lib/richtext-renderer-v2';
import RichTextMark from '../../lib/richtext-renderer-v2/astro/RichTextMark.astro';

interface Props {
  node: StoryblokRichTextJson;
}
const { node } = Astro.props;
const { attrs, content } = node;
// console.log(Astro.props);
---

<h1>
  {content?.map((node) => <RichTextMark node={node} />)}
</h1>
